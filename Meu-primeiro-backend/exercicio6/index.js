const nomeApp = "iFood Clone API";
const porta = 8080;

// Usando Template Strings com crases:
const mensagem = `🚀 [BACK-END] A API ${nomeApp} está rodando com sucesso na porta ${porta}!`;

console.log(mensagem);
